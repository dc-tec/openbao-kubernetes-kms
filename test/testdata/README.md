# Test Data

Golden fixtures cover provider configuration, Kubernetes encryption
configuration, JSON Web Token (JWT) authentication, key IDs, annotations,
additional authenticated data (AAD), and key registry state.
